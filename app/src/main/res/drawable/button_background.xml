<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/black"/> <!-- Transparent background -->
    <stroke android:width="4dp" android:color="@android:color/white"/> <!-- White outline -->
    <corners android:radius="16dp"/> <!-- Adjust the radius as needed -->
</shape>
